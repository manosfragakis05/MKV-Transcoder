[package]
name = "streaming-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
webm-iterable = "0.6" 
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cc = "1.0"